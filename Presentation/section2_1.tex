\documentclass{beamer}
\usepackage{tikz}
\usetikzlibrary{shapes}
\usepackage[utf8]{inputenc}
\usetheme{Madrid}

\tikzstyle{oval} = [ellipse,draw]
\tikzstyle{rectangle} = [rectangle,draw,text centered]
\tikzstyle{diamond} = [diamond,aspect=4,draw]
\tikzstyle{paralellogram} = [trapezium, trapezium left angle=70, trapezium right angle=110,draw]

\section{What is a UIS?}
\begin{frame}{What is a UIS?}
	\begin{figure}[h]
	\centering
		\begin{tikzpicture}
		%\node[startstop](start) {Start};
		%\node[normal,below of=start, text width=1cm](i_sum) {i=0  sum=0};
		%\node[decision,below of=i_sum,yshift=-0.5cm](while) {while i $ \neq $ 0};
		%\node[io,below of=while](input) {Input i};
		%\node[normal,below of=input,text width=2cm](sum_incr) {sum=i+sum increment i};
		%\node[normal,below of=sum_incr,text width=2cm](print) {Print sum};
		%\node[startstop,below of=print,right of=print,xshift=2cm](end) {End};
		
		%\draw[->] (start) -- (i_sum);
		%\draw[->] (i_sum) -- (while);
		%\draw[->] (while) -- node[right] {T} (input);
		%\draw[->] (print.west) -- ++(-3,0) |- (while.west);
		
		\node[rectangle](Application) {Application};
		\end{tikzpicture}
	\end{figure}
\end{frame}

\end{document}
